// For each button, add an event listener to send a message to the content script

document.addEventListener('DOMContentLoaded', function() {
    // Get the buttons
    const searchBtn = document.getElementById('search');
    const summarizeBtn = document.getElementById('summarize');
    const checkRelevanceBtn = document.getElementById('checkRelevance');
    const savePageBtn = document.getElementById('savePage');
    const viewSavedBtn = document.getElementById('viewSaved');
});